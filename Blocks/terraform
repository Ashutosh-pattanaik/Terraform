Defines Terraform settings like required providers and versions.

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

Purpose:

Define provider versions

Configure backend

Set Terraform requirements
