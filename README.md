# Terraform


# BLOCK
this folder contains different types of tgerraform blocks
Simple Terraform Flow
terraform block  → settings
provider block   → cloud connection
resource block   → create infrastructure
data block       → read existing infrastructure
variable block   → input values
output block     → show results
